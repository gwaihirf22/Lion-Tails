/**
 * What every AI-written story says about how it was made.
 *
 * Blake: "a disclaimer to every story that is created about AI generation and
 * that it is not always factual." Shown by the READER, the print path and the
 * download -- never written into story.content -- so every story already in a
 * library has it, the universe summariser never reads it as an event, and a
 * parent edit cannot remove it. Not shown on a built-in story, which a person
 * wrote.
 */
export const AI_NOTE_TITLE = "Written with AI.";

export const AI_NOTE =
  "This story was written with the help of artificial intelligence, and AI can get things wrong " +
  "— a name, a date, a detail, even what a passage says. Where it touches the Bible or history, " +
  "read the real account too; the sources in Further reading are a good place to start.";
